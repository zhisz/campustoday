class CampusAuth:
    def login(self):
        raise RuntimeError("Current institution login flow is not configured")

    def refresh(self):
        raise RuntimeError("Current institution login flow is not configured")

